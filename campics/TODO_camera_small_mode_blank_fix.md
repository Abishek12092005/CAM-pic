# TODO - Fix blank/black camera view + reduce camera height

- [ ] Add CSS to ensure `#video` shows framed content (height/width sizing, min-height, and prevent flex issues).
- [ ] Add CSS to make camera area shorter on mobile (smaller max-height).
- [ ] Optional: add a video background/hidden state until ready.
- [ ] Manual test on `/camera`.

